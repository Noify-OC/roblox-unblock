# roblox-unblock

## Installation
```bash
go install github.com/Noify-OC/roblox-unblock@latest
```

## Setup
**The program requires the following variables to either be set in an .env file or in the environment**

```env
COOKIE=""
X_CSRF_TOKEN=""
```

## Usage
```bash
roblox-unblock --help
```
